{{form_start(usersForm)}}
    {{form_row(usersForm.email)}}
    
    {{form_row(usersForm.password)}}
    {{form_row(usersForm.nom)}}
    {{form_row(usersForm.prenom)}}
    {{form_row(usersForm.login)}}
  
   <center> <button type="submit" class="btn btn-success">{{ button_label|default('Ajouter') }}</button></center>
{{form_end(usersForm)}}
